/* 我的订单页 - editorial 风格
   主要组件（hero / mag-card / mag-empty / status-pill / price-block 等）已在 app.wxss 定义 */

.status-cn {
  font-size: 22rpx;
  color: #8A847B;
  letter-spacing: 2rpx;
  align-self: center;
}
